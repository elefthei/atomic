(no external research applicable)

The two files in scope — `install.ps1` (a PowerShell bootstrap installer that installs `bun` and then installs the `@bastani/atomic` npm package globally) and `src/version.ts` (a 7-line module that re-exports the version string from `package.json`) — contain no deterministic workflow logic and depend on no external libraries whose documentation would illuminate how Atomic implements deterministic workflows. The `Invoke-Step` pattern visible in `install.ps1` is a PowerShell UI convention for sequential, animated step execution during installation, not a workflow orchestration primitive; and `version.ts` is a trivial constant export with no runtime dependencies. Fetching external documentation for `bun`, `@bastani/atomic`, or PowerShell scripting conventions would not advance the research question.
