(no external research applicable)

The partition's files (`install.sh` and `src/scripts/`) rely exclusively on standard POSIX shell utilities, Bun's built-in `$` shell template API, and the `git` CLI. None of these require external documentation lookup to understand how deterministic workflows are implemented: the locator findings already describe all relevant mechanisms — version pinning via `bun install -g`, semver regex parsing in `bump-version.ts`, deterministic subprocess execution via Bun's `$` API across all four script files, and the constant-driven `ZIP_INCLUDES` array in `bundle-configs.ts`. The behavior is fully self-contained within the partition's own source code and does not depend on third-party library APIs with non-obvious or externally documented semantics.
