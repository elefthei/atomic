(no external research applicable)
The `.agents/` partition is purely markdown skill documentation; answering how Atomic's deterministic workflows function requires reading those local docs, not fetching external pages.
