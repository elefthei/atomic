(no external research applicable)

The `tests/` directory uses bun:test with mocked provider SDK modules to validate Atomic CLI's internal logic; understanding deterministic workflows requires reading the local source and test files rather than fetching external documentation.
