(no external research applicable)

The assigned scope is `rest-api/` — a standalone Bun.serve REST CRUD sample (9 files, 1,168 LOC). This partition contains straightforward stateless HTTP request handlers for item create, read, update, and delete operations. The locator analysis confirms that no deterministic workflow patterns exist anywhere in this scope.

Atomic's deterministic workflow machinery — sequenced, reproducible agent execution pipelines — is an orchestration-layer concern that lives outside a simple CRUD API. Fetching external documentation about that mechanism would produce findings with zero applicability to the code in this partition. Online research is therefore skipped.
